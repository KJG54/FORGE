[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "forge-governance"
version = "1.0.0"
description = "A local-first governance framework for human-directed, AI-assisted work"
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
authors = [{ name = "FORGE contributors" }]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "pydantic>=2.10,<3",
  "pyyaml>=6.0.2,<7",
  "typer>=0.15,<1",
]

[project.optional-dependencies]
dev = [
  "build>=1.2.2,<2",
  "pyright>=1.1.390,<2",
  "pytest>=8.3,<9",
  "ruff>=0.9,<1",
]

[project.scripts]
forge = "forge.cli.entrypoint:main"

[tool.hatch.build]
exclude = ["/.forge/local/**"]
skip-excluded-dirs = true

[tool.hatch.build.targets.wheel]
packages = ["src/forge"]

[tool.hatch.build.targets.sdist]
exclude = [
  "/.forge/local/**",
  "/docs/history/milestones/local-v1-l9.md",
  "/release/local-production-v1/candidate-manifest.json",
  "/release/local-production-v1/extended-testing-plan.md",
  "/release/local-production-v1/friction-report.md",
  "/release/local-production-v1/SHA256SUMS",
  "/release/local-production-v1/validation-report.md",
]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.pyright]
include = ["src", "tests", "tools"]
pythonVersion = "3.12"
typeCheckingMode = "strict"
