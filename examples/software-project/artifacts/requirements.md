# Requirements

1. Recognize `added`, `changed`, and `fixed` categories.
2. Render headings in that fixed order.
3. Omit empty categories.
4. Preserve entry order and exact title text.
5. Return a visible error for an unsupported category.
6. Produce identical output for identical input.

The example does not cover localization, hostile input, packaging, or terminal presentation.
